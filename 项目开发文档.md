# OCCT 户型图可视化项目开发文档

## 项目概述

基于 Three.js 的 3D/2D 户型图可视化应用，支持 3D 渲染、2D 彩平图、CSG 布尔运算、材质编辑与软装渲染。

**架构要点：纯前端，无后端服务，不依赖 WebAssembly。** 所有几何运算都在客户端用 JS 完成。

### 技术栈
- **渲染**：Three.js
- **二维多边形运算**：clipper-lib（偏移 + 并集）
- **CSG**：three-bvh-csg + three-mesh-bvh
- **UI**：原生 DOM，无框架
- **构建**：Vite

> UI 全部是原生 DOM 操作（无 `.jsx`，`index.html` 也没有挂载点），没有前端框架。
> `three-mesh-bvh` 虽然源码里没直接 import，但它是 `three-bvh-csg` 的
> peer dependency，**必须保留**。

## 架构

### 双场景架构（App.js）

同时管理 3D 与 2D 两套场景：

```javascript
class OCCTApp {
    constructor() {
        this.sceneManager3D = null;  // 3D 场景管理器
        this.sceneManager2D = null;  // 2D 场景管理器
        this.currentView = '3d';     // 当前视图模式
        this.currentMode = 'view';   // 操作模式 (view/edit)
    }
}
```

### 启动流程

```
DOMContentLoaded
  → DataSourcePicker.show()      选数据（本地文件 / 内置示例），并做配套校验
  → geometryService.setDataSource()
  → new OCCTApp() → init()
      → geometryService.init()   加载并解析户型 JSON
      → loadData()               取外轮廓/房间/门窗，交给渲染器
      → startRenderLoop()
```

### 几何服务（core/GeometryService.js）

承接原先 `server.js`（Node + Express）的全部职责。对外方法与原来的 HTTP 端点一一对应：

| 方法 | 原端点 | 说明 |
|---|---|---|
| `getOutline()` | `GET /outline` | 房间轮廓各自外扩墙厚 240mm → 求并集 → 取边界环（`outlineRings`：外环 + 内环） |
| `getRooms()` | `GET /rooms` | 房间数据 |
| `getDoorsAndWindows()` | `GET /doors_and_windows` | `processed_*` 为外扩 15mm 的挖洞用轮廓；`doors`/`windows` 为原始显示轮廓 |
| `getSoftlists()` | `GET /softlists` | 图例清单 |
| `getSoftlistPoints(id)` | `GET /softlists_points?id=` | 按 id 取图例几何 |

### 二维多边形运算（core/Polygon2D.js）

`getOutline` 与 `getDoorsAndWindows` 需要的其实只有两种**二维**操作：多边形外扩、求并集。
由 clipper-lib 实现。

这里曾经用的是 OpenCascade.js（wasm）。但所有输入都在 z=0 平面上（弧线在 `json_parse`
里已被采样成折线），也就是说一个 50MB 的 CAD 内核只是被当成二维多边形库在用，还要付
5 秒的 wasm 初始化。换成 Clipper 后结果等价（外轮廓包围盒差 <0.01mm），几何运算从
6064ms 降到 13ms，首屏从 8.6 秒降到 2.1 秒。

两个必须注意的点：

- **绕向**：Clipper 会把输出路径规范化成固定绕向，而下游 `ExtrudeGeometry` → CSG 依赖
  绕向判定内外。绕向反了，挤出体的法线朝内，布尔运算就挖不出洞——而且不报错。
  `matchWinding()` 负责让结果与输入保持一致。
- **多环**：并集的边界通常不止一个环（外环 + 房间之间的空隙形成的内环）。必须原样交给
  下游由 `THREE.Shape` 的 holes 表达，不能压平进一条路径。

### 数据来源（core/DataSource.js）

- `BundledDataSource` — 内置示例，从 `public/data/` fetch
- `LocalFileDataSource` — 用户在选择界面选中的本地文件

`GeometryService` 只依赖这个抽象，不再写死路径。

## 数据

### 需要哪些文件

渲染一套户型需要两样东西，**且二者配套，必须来自同一次解析**：

1. **户型 JSON** — 房间轮廓、门窗，以及图例清单（每个实例的 TypeId 与宽/长）
2. **parsed_dxf/{TypeId}_{序号}.json** — 每个图例实例的几何（polylines + wipeouts）

### 关键约束：文件名里的序号是解析顺序下标

`json_parse.js` 生成 id 的方式是 `TypeId + '_' + dxf_num++`，其中 `dxf_num` 是一个跨
soft_list / door_list / window_list 的全局递增计数器。因此：

- **图例清单包含软装、门、窗三类**（示例数据：62 + 15 + 9 = 86 项）
- 文件名与特定户型 JSON 的**条目顺序绑死**，换 JSON 必须换整套 `parsed_dxf`
- `freestyle`（自由绘制）图例的点数据直接在户型 JSON 里，不需要几何文件

配错不会报错，只会静默 404 缺图，所以 `DataSourcePicker` 在渲染前会把 JSON 解析出的
所有 id 逐个对到文件上，当场列出缺失项。

### 为什么不需要原始 .dxf

`parsed_dxf` 不是 DXF 的直接解析结果。外部解析器会按每个实例的宽/长对 DXF 图例做
**参数化拉伸**——同一 TypeId 的不同实例会被拉成不同尺寸（例如 TypeId `20cc` 的两个实例，
几何分别是 1500×380 和 1410×480，且两端 20mm 的边距都被保留，说明是保边距拉中段，
而非等比缩放）。

这套拉伸逻辑在外部解析器里，不在 DXF 文件里，浏览器无法自行还原。所以哪怕引入 DXF
解析库也没用——拿到的会是原始尺寸的图例，尺寸全错。

```
{TypeId}.dxf 图例库  ┐
                     ├─→ [外部解析器：参数化拉伸] ─→ parsed_dxf/*.json
户型 JSON（含实例尺寸）┘
```

外部解析器不是本仓库的一部分，也**不是运行本应用的前提**——只要数据已经解析好。

## 渲染系统

### 3D 渲染器（components/RoomRenderer.js）

CSG 统一使用 `three-bvh-csg`。四条必须遵守的规则：

**1. 合并后一次减，不要逐个累积。**
房间之间互不相交，把几何合并成一个 BufferGeometry（纯缓冲区拼接，零布尔成本）后一次
减掉。逐个累积会让被减数不断膨胀，每一轮都要重建一次 BVH——这是原实现最大的性能问题。

**2. 但减数不能无脑合并。**
门窗为了挖穿墙体各自外扩了 15mm，相邻的会互相重叠。把重叠的实体拼进同一个缓冲区会得到
**非流形**几何，BVH 的内外判定随即失效、洞根本挖不出来。因此先用包围盒把减数贪心分组成
互不重叠的批次（`groupDisjoint`），每批合并后减一次。实际数据下通常只分出 2~3 组。

**3. 挖洞必须用 `processed_*`（外扩版）。**
用原始尺寸的门窗，洞壁会与门窗自身的面完全共面，导致 z-fighting（表现为放射状条纹）。
`processed_doors` / `processed_windows` 就是为此准备的。

**4. 每次布尔前必须清理退化三角形。**
three-bvh-csg 的输出里会夹带零面积三角形。把这样的结果再喂回去做下一次布尔（本项目正是
链式布尔：外轮廓先减房间、再减门窗），它在算面法线时会拿到 `null` 并抛出
`Cannot read properties of null (reading 'dot')`——而这个异常会被 CSG 的 catch 吞掉，
表现为「洞静默地没挖出来」。`bakeGeometry()` 末尾的 `removeDegenerateTriangles()` 负责此事。

其他：
- `bakeGeometry()` 把 mesh 变换烘焙进几何并统一属性集——`WallFactory` 直接用世界坐标建几何，
  而 `DoorWindowFactory` 把 z 偏移挂在 `mesh.position` 上，不统一就无法安全合并
- `Evaluator` 全局共享，无需每次布尔都新建
- 门窗包围盒预计算一次供所有墙面复用

**注意**：`WallFactory` 生成的墙体是零厚度的曲面带（不是实体）。对它做布尔实际上是"用实体
裁剪开放曲面"，三维 CSG 能正确处理，但对容差敏感。

### 2D 渲染器（components/PlanRenderer.js）

**不要对平面用 CSG。** 用 `THREE.Shape.holes`：外轮廓为 shape，房间/门/窗作为 holes，
由 `ShapeGeometry` 一次三角化完成。

零厚度的平面没有内外之分，布尔运算在这种退化情形下只能空转——实测单次减法 1597ms，
是同一引擎在 3D 实体上的近 100 倍。原实现在这里做了 31 次布尔，耗时 49.5 秒。

### 软装渲染器（components/SoftlistRenderer.js）

一个 DXF 图例常有上百条 polyline，每条平均只有 3 个点。若各自成 `THREE.Line` 并配独立
材质，整个场景会有上万次 draw call 去画几万个顶点，纯属调用开销。

按图例实例把所有 polyline 合并成单个 `LineSegments`（共享一个材质）。选择器按 group 拾取、
按 child 换材质，合并后行为不变。

图例的几何文件并行拉取（`Promise.all`），不要在 for 循环里逐个 `await`。

## 性能

### 当前各阶段耗时（示例户型：7 房间 / 15 门 / 9 窗 / 62 软装）

| 阶段 | 耗时 |
|---|---|
| 加载并解析户型 JSON | ~0.1 s |
| 几何服务（外轮廓并集等） | ~0.01 s |
| 3D 建模（RoomRenderer.render） | ~0.76 s |
| 2D 彩平图（含软装） | ~0.14 s |

### 优化历史

| 项 | 之前 | 之后 |
|---|---|---|
| 2D 彩平图 | 49.9 s | 0.14 s |
| 3D 建模 | 2.65 s | 0.76 s |
| 2D draw call | 10295 | 81 |
| 外轮廓顶点数 | 76,236 | 22,572 |

### 经验教训

- **调用次数多 ≠ 耗时多。** `isValidMesh` 的全顶点扫描累计扫了 287 万个顶点，实测仅 19ms；
  2910 次 `computeBoundingBox` 实测仅 11ms。都是噪声级别。**先剖析，再优化。**
- **真正的瓶颈是算法层面的**：对平面做 CSG、逐个累积布尔、每条线一个 draw call。

### 尚未优化

目前首屏约 2 秒，主要是解析数据与建模，没有明显的单点瓶颈。

## 交互系统

- **选择器**：`WallSelector`（3D 墙面）、`Room2DSelector`（2D 房间）、
  `Softlist2DSelector`（2D 软装）、`SelectionManager`（3D 对象）
- **拖拽**：`DragDropManager` — 材质拖拽应用、3D 模型拖拽创建、实时预览
- **材质编辑**：`MaterialSidebar`（3D 材质库）、`DXFMaterialSidebar`（2D 软装材质）

## 代码约定

### 命名
- 组件类 PascalCase，方法与变量 camelCase，常量 UPPER_SNAKE_CASE
- 文件名 PascalCase.js

### 内存管理
- Three.js 对象需显式 `dispose()`
- 事件监听器在组件销毁时移除

### 日志
不要在热路径（布尔运算、逐 mesh 循环）里 `console.log`，尤其不要打印整个 mesh 对象——
它会保留引用阻止 GC。原实现有 3437 次 console 调用，实测约占 render 耗时的 13%。

## 已知问题与后续计划

### 待优化
- [ ] 每个 CSG 结果 mesh 各持有独立材质，无法合批

### 功能
- [ ] 材质库扩展与自定义材质上传
- [ ] 场景导出（GLB/GLTF）
- [ ] 撤销/重做
- [ ] 单元测试

---

**文档更新日期**：2026-07-14
