# OCCT 户型图可视化项目开发文档

## 项目概述

OCCT 户型图可视化是一个基于 Three.js 和 OpenCascade.js 的 3D/2D 户型图可视化应用。项目支持户型图的 3D 渲染、2D 彩平图显示、CSG 布尔运算、材质编辑、软装渲染等功能。

### 技术栈
- **前端**: Three.js, React (ES模块), Vite
- **后端**: Node.js, Express
- **几何引擎**: OpenCascade.js, Three-CSG, Three-BVH-CSG
- **DXF解析**: @ke/kedxf
- **工具库**: Lodash, Vecks

## 项目结构

```
F:\vscode_project\occt\
├── package.json                    # 项目配置和依赖
├── server.js                      # Node.js 后端服务器
├── index.html                     # 主页面入口
├── json_parse.js                  # JSON数据解析器
├── colorplane.js                  # 彩平图相关
├── src/                          # 源码目录
│   ├── App.js                    # 主应用程序
│   ├── core/                     # 核心模块
│   │   ├── SceneManager.js       # 3D场景管理
│   │   └── Scene2DManager.js     # 2D场景管理
│   ├── components/               # 组件目录
│   │   ├── RoomRenderer.js       # 房间3D渲染器
│   │   ├── PlanRenderer.js       # 2D平面渲染器
│   │   ├── MaterialSidebar.js    # 3D材质侧边栏
│   │   ├── DXFMaterialSidebar.js # 2D材质编辑器
│   │   ├── WallSelector.js       # 墙面选择器
│   │   ├── DragDropManager.js    # 拖拽管理器
│   │   ├── SelectionManager.js   # 3D选择管理器
│   │   ├── Room2DSelector.js     # 2D房间选择器
│   │   ├── Softlist2DSelector.js # 2D软装选择器
│   │   ├── SoftlistRenderer.js   # 软装渲染器
│   │   ├── WallFactory.js        # 墙体工厂
│   │   ├── FloorFactory.js       # 地板工厂
│   │   └── DoorWindowFactory.js  # 门窗工厂
│   └── utils/                    # 工具类
│       ├── CSGEngineManager.js   # CSG引擎管理
│       ├── CSGMeshOperations.js  # CSG网格操作
│       └── SVGLoader.js          # SVG加载器
├── public/                       # 静态资源
│   └── mx_250804/               # 材质资源包
│       ├── T_YZ_BB_305311035_030/
│       ├── T_YZ_DZ_305311142_053/
│       └── ...
└── node_modules/                # 依赖包
```

## 核心功能模块

### 1. 双场景架构 (App.js)

项目采用双场景架构，同时管理 3D 和 2D 视图：

- **3D 场景**: 使用 `SceneManager` 进行 WebGL 渲染
- **2D 场景**: 使用 `Scene2DManager` 进行平面图渲染
- **视图切换**: 支持在 3D 视图和 2D 彩平图之间切换

```javascript
class OCCTApp {
    constructor() {
        this.sceneManager3D = null;  // 3D场景管理器
        this.sceneManager2D = null;  // 2D场景管理器
        this.currentView = '3d';     // 当前视图模式
        this.currentMode = 'view';   // 操作模式 (view/edit)
    }
}
```

### 2. 后端数据服务 (server.js)

基于 Express 的 Node.js 后端，提供以下 API 端点：

#### API 端点
- `GET /outline` - 获取户型外轮廓数据（使用 OpenCascade 进行布尔运算）
- `GET /rooms` - 获取房间数据
- `GET /doors_and_windows` - 获取门窗数据（包含弧形采样和偏移处理）
- `GET /softlists` - 获取软装列表
- `GET /softlists_points?id=xxx` - 根据ID获取具体软装点数据

#### 核心功能
- **OpenCascade.js 集成**: 进行精确的几何计算和布尔运算
- **DXF数据解析**: 解析 CAD 文件生成的 JSON 数据
- **弧形采样**: 处理门窗的弧形段，转换为多边形近似
- **几何偏移**: 对门窗进行向外偏移以便挖洞操作

### 3. 渲染系统

#### 3D 渲染器 (RoomRenderer.js)
- **CSG 布尔运算**: 支持多种 CSG 引擎 (three-csgmesh, three-bvh-csg)
- **渐进式渲染**: 分步进行门窗挖洞操作，显示进度
- **材质系统**: 支持 PBR 材质和纹理映射

#### 2D 渲染器 (PlanRenderer.js)
- **平面图生成**: 基于房间数据生成 2D 平面图
- **彩平图渲染**: 支持不同房间的颜色区分

#### 软装渲染器 (SoftlistRenderer.js)
- **DXF 解析**: 解析软装的 DXF 数据
- **批量渲染**: 高效渲染大量软装物品
- **材质编辑**: 支持软装材质的实时编辑

### 4. 交互系统

#### 选择器系统
- **WallSelector.js**: 3D 墙面选择
- **Room2DSelector.js**: 2D 房间选择
- **Softlist2DSelector.js**: 2D 软装选择
- **SelectionManager.js**: 3D 对象选择管理

#### 拖拽系统 (DragDropManager.js)
- 材质拖拽应用
- 3D 模型拖拽创建
- 实时预览反馈

#### 材质编辑系统
- **MaterialSidebar.js**: 3D 材质库和编辑器
- **DXFMaterialSidebar.js**: 2D 软装材质编辑器

## 开发状态和进度

### ✅ 已完成功能
1. **基础架构**
   - 双场景管理系统完成
   - Express 后端 API 服务完成
   - 前端模块化架构完成

2. **3D 渲染功能**
   - 房间 3D 几何体生成
   - CSG 门窗挖洞操作
   - 材质系统和 PBR 渲染
   - 3D 对象选择和编辑

3. **2D 渲染功能**
   - 2D 彩平图生成
   - 房间区域选择
   - 软装 DXF 解析和渲染

4. **交互功能**
   - 视图切换 (3D/2D)
   - 操作模式切换 (查看/编辑)
   - 拖拽材质应用
   - 右键上下文菜单

5. **数据处理**
   - DXF/JSON 数据解析
   - 弧形几何采样
   - OpenCascade 几何运算

### 🚧 当前开发阶段

项目目前处于**功能整合和优化阶段**：

1. **软装材质编辑器** - 基本功能已实现，正在完善交互体验
2. **性能优化** - 对大量软装渲染进行优化
3. **CSG 引擎调优** - 优化门窗挖洞的性能和稳定性
4. **UI/UX 改进** - 完善材质侧边栏和编辑界面

### 🔄 下一步开发计划

1. **功能完善**
   - [ ] 材质库扩展和自定义材质上传
   - [ ] 软装库管理和分类
   - [ ] 场景导出功能 (GLB/GLTF)
   - [ ] 材质预设保存和加载

2. **性能优化**
   - [ ] 大场景渲染优化
   - [ ] 内存管理优化
   - [ ] CSG 计算性能提升
   - [ ] 软装渲染批处理优化

3. **用户体验**
   - [ ] 操作提示和帮助系统
   - [ ] 撤销/重做功能
   - [ ] 键盘快捷键扩展
   - [ ] 触摸设备支持

4. **稳定性**
   - [ ] 错误处理完善
   - [ ] 数据验证加强
   - [ ] 异常恢复机制
   - [ ] 单元测试覆盖

## 技术特点

### 1. 几何计算
- 使用 OpenCascade.js 进行精确的 CAD 级几何运算
- 支持复杂多边形的布尔运算和偏移操作
- 弧形几何的高精度采样和近似

### 2. 渲染性能
- Three.js WebGL 渲染管线
- 实例化渲染减少 Draw Call
- 材质合批和纹理图集优化
- LOD (Level of Detail) 距离优化

### 3. 内存管理
- OpenCascade 对象的及时清理
- Three.js 几何体和材质的内存回收
- 大场景的分块加载和卸载

### 4. 数据流
- 前后端分离架构
- RESTful API 设计
- 数据缓存和增量更新
- 异步加载和渐进式渲染

## 部署说明

### 开发环境启动
```bash
# 启动后端服务器
npm run dev

# 或启动 React 开发服务器
npm run dev-react

# 构建生产版本
npm run build
```

### 环境要求
- Node.js 16+
- 现代浏览器 (支持 WebGL 2.0)
- 8GB+ 内存 (推荐)

### 外部依赖
- DXF 解析器可执行文件: `F:/vscode_project/dxfparse/Source/CadToolTest/Debug/CadToolTest.exe`
- 材质资源包: `public/mx_250804/`
- 软装数据文件: `C:/Users/User/desktop/parsed_dxf/`

## 代码约定

### 模块化设计
- 每个功能组件独立成类
- 清晰的接口定义和事件回调
- 统一的错误处理模式

### 命名规范
- 组件类使用 PascalCase
- 方法和变量使用 camelCase
- 常量使用 UPPER_SNAKE_CASE
- 文件名使用 PascalCase.js

### 内存管理
- 所有 Three.js 对象需要显式 dispose()
- OpenCascade 对象需要 delete() 清理
- 事件监听器需要在组件销毁时移除

## 调试和维护

### 关键调试信息
- 浏览器控制台输出各组件的初始化和运行状态
- FPS 计数器监控渲染性能
- CSG 操作进度和错误日志
- API 请求状态和数据验证

### 常见问题
1. **CSG 操作失败**: 检查几何体有效性和数值精度
2. **内存泄漏**: 确认所有 Three.js 对象正确释放
3. **渲染卡顿**: 检查多边形数量和材质复杂度
4. **数据解析错误**: 验证 DXF/JSON 数据格式

---

**文档更新日期**: 2025-08-12
**项目版本**: 开发版本
**维护者**: AI 开发助手

此文档将随项目开发进度持续更新。如需了解特定功能的实现细节，请查看对应源码文件中的注释。